  // $(document).ready(function(){
  //     var filename = document.getElementById('custId').value;
  //     url = `./uploads/${filename}`;
  //     console.log(url);
  //     $( ".show-img" ).append(`<img class="responsive-img" src=${url} alt="blah">`);
  // });


// loop within the loop

// To be debugged: loop through the blogs and for each blog loops through the files and fetch filename to display the image
// const loopFiles = (myDoc) =>{
//    myDoc.forEach(return showFileName(file))
// }
//
// const showFileName = (file) => {
//   file.forEach(return filename)
// }
// const filename = db.blogs.find().forEach(loopFiles(myDoc))
//
//
//
// db.blogs.find().forEach(function(blog) {
//   blog.files.forEach(function(file) {
//     return (file.filename)
//   })
// })
